package openfl.text;


class TextLineMetricsTest { public static function __init__ () { Mocha.describe ("Haxe | TextLineMetrics", function () {
	
	
	Mocha.it ("ascent", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		var exists = textLineMetrics.ascent;
		
		Assert.notEqual (exists, null);
		
	});
	
	
	Mocha.it ("descent", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		var exists = textLineMetrics.descent;
		
		Assert.notEqual (exists, null);
		
	});
	
	
	Mocha.it ("height", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		var exists = textLineMetrics.height;
		
		Assert.notEqual (exists, null);
		
	});
	
	
	Mocha.it ("leading", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		var exists = textLineMetrics.leading;
		
		Assert.notEqual (exists, null);
		
	});
	
	
	Mocha.it ("width", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		var exists = textLineMetrics.width;
		
		Assert.notEqual (exists, null);
		
	});
	
	
	Mocha.it ("x", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		var exists = textLineMetrics.x;
		
		Assert.notEqual (exists, null);
		
	});
	
	
	Mocha.it ("new", function () {
		
		// TODO: Confirm functionality
		
		var textLineMetrics = new TextLineMetrics (0, 0, 0, 0, 0, 0);
		Assert.notEqual (textLineMetrics, null);
		
	});
	
	
}); }}